## Thread

**An independent execution unit within a process, the smallest unit of CPU scheduling. The basic unit of program execution.
Thread pool: A container that stores multiple thread objects, enabling thread object reuse.**
